#
# Read the Docs uses pip to install your Python packages.
# If you have local dependencies, you can install them
# from a Git repository or a repository manager.
#
# Pip supports installing packages from a Git repository using the URI form:
#
#    git+https://gitprovider.com/user/project.git@{version}
#

# This library is actually available on pypi so take it from there
# using the latest version available
carlogtt-python-library
